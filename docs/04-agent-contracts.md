# Agent Contracts

Agent contracts are placeholders in Stage 0. Future stages should define typed inputs, outputs, confidence semantics, evidence requirements, and approval handoff behavior before implementing autonomous CRM recommendations.

## Expected Contract Properties

- Deterministic schema validation with Zod.
- Evidence references for every material recommendation.
- Explicit confidence and uncertainty fields.
- Human-readable rationale.
- Safe failure modes when required evidence is missing.
